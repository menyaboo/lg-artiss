export interface Services {
  id: number,
  bg: string,
  img: string,
  description: string,
}

export interface Gallery {
  id: number,
  url: string,
}

export interface Partners {
  id: number,
  url: string,
}